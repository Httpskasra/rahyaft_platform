"use client"; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {recruitmentApi} from '@/lib/api/recruitment';
export default function Careers(){const [jobs,setJobs]=useState<any[]>([]);useEffect(()=>{recruitmentApi.publicJobs().then(setJobs)},[]);return <main dir="rtl" className="min-h-screen bg-gray-50 p-6"><div className="mx-auto max-w-5xl"><h1 className="mb-2 text-3xl font-black">فرصت‌های همکاری</h1><p className="mb-8 text-gray-500">برای پیوستن به سازمان، موقعیت مناسب را انتخاب کنید.</p><div className="grid gap-4 md:grid-cols-2">{jobs.map(j=><article key={j.id} className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">{j.title}</h2><p className="mt-2 line-clamp-3 text-gray-600">{j.description}</p><Link className="mt-5 inline-block rounded-xl bg-indigo-600 px-4 py-2 text-white" href={`/careers/${j.slug}`}>مشاهده و ثبت درخواست</Link></article>)}</div></div></main>}
